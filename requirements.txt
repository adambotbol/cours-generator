# Web
flask>=3.0
jinja2>=3.1

# Google AI
google-genai>=1.0

# NotebookLM automation (local only — requires Google browser session)
notebooklm-py

# PDF
pymupdf>=1.24

# Utils
python-dotenv>=1.0

# Supabase (optionnel — décommenter lors de la migration Phase 2)
# supabase>=2.0
